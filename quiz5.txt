Quiz 5 - Hugo Genesse

a)
b) Pour les raisons développées à la question c, toutes les variables
sont dépendantes.
c)

unsigned int fib(unsigned int n){ 
unsigned int i = n - 1, a = 1, b = 0, c = 0, d = 1, t;
if (n <= 0) 
return 0;
while (i > 0){
while (i % 2 == 0){
t = d*(2*c + d);
c = c*c + d*d;
d = t;
i = i / 2;
}
t = d*(b + a) + c*b;
a = d*b + c*a;
b = t;
I--;
}
return a + b;
}

Je crois que le slicing de fibonacci est égal au même programme car il
y a dépendance de données entre toutes les variables en tenant compte
des dépendances de contrôle. Je crois que, parce que nous pouvons
retourner a + b dans les cas où n est plus grand que 0 et que nous
n’avons aucune information préalable à n. Cela cause une dépendance de
données car les valeurs a et b sont calculées avec les autres
variables et i sert de variable de contrôle et elle dépend elle-même
de n.

d) Une des applications du slicing est la spécialisation d’un
programme. En analysant les dépendances de contrôle et de données, on
peut séparer un programme qui possède plusieurs fonctionnalité et
ainsi le rendre spécialisé à une tâche.
